# POST requests contain: email, intent, source, and submittedAt.
# Point this at your CRM, newsletter provider, or serverless lead-capture endpoint.
VITE_LEAD_CAPTURE_ENDPOINT=https://example.com/api/leads
